# Backend configuration (all optional — sensible defaults are built in)

# Set to require an X-API-Key header on prediction endpoints. Leave blank to disable auth.
API_KEY=

# Comma-separated allowed CORS origins, or "*" for any (default).
FRONTEND_ORIGINS=http://localhost:3000

# Port for the API server.
PORT=8000

ENVIRONMENT=development
