# Frontend configuration

# Base URL of the churn prediction API.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: only needed if the backend sets API_KEY.
NEXT_PUBLIC_API_KEY=

# Force the built-in demo/mock data instead of calling the API.
NEXT_PUBLIC_USE_MOCK_API=false
